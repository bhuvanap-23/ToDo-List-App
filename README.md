# ToDo-List-App

This application tracks the to-do tasks. We can add the tasks that we have in line that can be marked completed and deleted once it's done. It uses MERN(mongoDB as backend, ExpressJS and NodeJS as server side, React as frontend) stack for implementing this application.
